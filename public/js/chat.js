const $=id=>document.getElementById(id);let conversations=[],users=[],activeId=null,lastMessageId=null,pollTimer=null,loading=false;
async function api(url,opts={}){const r=await fetch(url,opts);let d={};try{d=await r.json()}catch{}if(!r.ok)throw new Error(d.error||'Request failed');return d}
function esc(s){return String(s??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}
function fmtTime(v){if(!v)return'';const d=new Date(v);const now=new Date();return d.toDateString()===now.toDateString()?d.toLocaleTimeString([], {hour:'2-digit',minute:'2-digit'}):d.toLocaleDateString([], {day:'2-digit',month:'short'})}
function avatar(name){return esc((String(name||'W').trim()[0]||'W').toUpperCase())}
async function init(){try{const me=await api('/api/me');if(!me.user)return location='/login.html';$('chatUser').textContent=me.user.name||'';$('backPortal').href=me.user.role==='admin'?'/admin.html':'/student.html';await loadUsers();await loadConversations();bind();}catch(e){alert(e.message);location='/login.html'}}
async function loadUsers(){const d=await api('/api/chat/users');users=d.users||[];renderUsers();}
async function loadConversations(){const d=await api('/api/chat/conversations');conversations=d.conversations||[];renderConversations();if(activeId){const c=conversations.find(x=>x.id===activeId);if(c){updateHeader(c);await loadMessages(true)}else closeRoom()}}
function renderConversations(){const q=($('chatSearch').value||'').toLowerCase();const list=conversations.filter(c=>(c.name+' '+c.lastMessage).toLowerCase().includes(q));$('conversationList').innerHTML=list.length?list.map(c=>`<div class="chat-row ${c.id===activeId?'active':''}" data-id="${c.id}"><div class="chat-avatar">${avatar(c.name)}</div><div class="chat-row-info"><div class="chat-row-top"><span class="chat-row-name">${esc(c.name)}${c.type==='group'?' 👥':''}</span><span class="chat-row-time">${fmtTime(c.lastMessageAt)}</span></div><div class="chat-row-preview">${esc(c.lastMessage||'Start a conversation')}</div></div>${c.unread?`<span class="chat-unread">${c.unread>99?'99+':c.unread}</span>`:''}</div>`).join(''):'<div class="chat-empty">No chats yet.<br>Start a conversation.</div>';document.querySelectorAll('.chat-row').forEach(x=>x.onclick=()=>openRoom(x.dataset.id))}
function renderUsers(){const q=($('userSearch').value||'').toLowerCase();const rows=users.filter(u=>(u.name+' '+u.email).toLowerCase().includes(q));$('userList').innerHTML=rows.length?rows.map(u=>`<div class="chat-user direct-user" data-id="${u.id}"><div class="chat-avatar">${avatar(u.name)}</div><div><div class="chat-user-name">${esc(u.name)}</div><div class="chat-user-role">${esc(u.role)}</div></div></div>`).join(''):'<div class="chat-empty">No people found.</div>';$('groupUserList').innerHTML=users.map(u=>`<label class="chat-user"><input type="checkbox" value="${u.id}"><div class="chat-avatar">${avatar(u.name)}</div><div><div class="chat-user-name">${esc(u.name)}</div><div class="chat-user-role">${esc(u.role)}</div></div></label>`).join('')||'<div class="chat-empty">No people available.</div>';document.querySelectorAll('.direct-user').forEach(x=>x.onclick=()=>startDirect(x.dataset.id))}
async function startDirect(id){try{const d=await api('/api/chat/direct',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({userId:id})});closeModal();await loadConversations();openRoom(d.conversation.id)}catch(e){alert(e.message)}}
async function openRoom(id){activeId=id;lastMessageId=null;const c=conversations.find(x=>x.id===id);if(!c)return;updateHeader(c);$('chatWelcome').classList.add('hidden');$('chatRoom').classList.remove('hidden');document.querySelector('.chat-shell').classList.add('room-open');renderConversations();$('messageList').innerHTML='<div class="chat-empty">Loading messages…</div>';await loadMessages(true);await markRead();}
function updateHeader(c){$('roomTitle').textContent=c.name;$('roomMeta').textContent=c.type==='group'?`${c.memberCount} members`:(c.members?.find(m=>m.id!==getMyId())?.role||'Direct chat');$('roomAvatar').textContent=String(c.name||'W')[0].toUpperCase()}
let myId='';function getMyId(){return myId}
async function loadMessages(full=false){if(!activeId||loading)return;loading=true;try{const d=await api(`/api/chat/conversations/${activeId}/messages${full||!lastMessageId?'':'?after='+encodeURIComponent(lastMessageId)}`);const msgs=d.messages||[];if(full){$('messageList').innerHTML='';}for(const m of msgs)appendMessage(m);if(msgs.length){lastMessageId=msgs[msgs.length-1].id;$('messageList').scrollTop=$('messageList').scrollHeight;}if(!full&&msgs.length)await markRead();}catch(e){if(full)$('messageList').innerHTML=`<div class="chat-empty">${esc(e.message)}</div>`}finally{loading=false}}
function appendMessage(m){if(document.querySelector(`[data-message-id="${m.id}"]`))return;const mine=m.senderId===getMyId();const wrap=document.createElement('div');wrap.className='chat-bubble-wrap '+(mine?'mine':'theirs');wrap.dataset.messageId=m.id;wrap.innerHTML=`<div class="chat-bubble">${!mine?`<div class="chat-sender">${esc(m.senderName)}</div>`:''}${esc(m.text)}<span class="chat-time">${fmtTime(m.createdAt)}${mine?' ✓':''}</span></div>`;$('messageList').appendChild(wrap)}
async function markRead(){if(activeId)try{await api(`/api/chat/conversations/${activeId}/read`,{method:'POST'})}catch{}}
async function sendMessage(e){e.preventDefault();const input=$('messageInput');const text=input.value.trim();if(!text||!activeId)return;input.disabled=true;try{const d=await api(`/api/chat/conversations/${activeId}/messages`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({text})});appendMessage(d.message);lastMessageId=d.message.id;input.value='';$('messageList').scrollTop=$('messageList').scrollHeight;await loadConversations()}catch(e){alert(e.message)}finally{input.disabled=false;input.focus()}}
async function createGroup(e){e.preventDefault();const name=$('groupName').value.trim();const ids=[...document.querySelectorAll('#groupUserList input:checked')].map(x=>x.value);try{const d=await api('/api/chat/groups',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name,memberIds:ids})});closeModal();$('groupName').value='';await loadConversations();openRoom(d.conversation.id)}catch(e){alert(e.message)}}
function showModal(){renderUsers();$('newChatModal').classList.remove('hidden');document.querySelector('[data-tab="direct"]').click()};function closeModal(){$('newChatModal').classList.add('hidden')}
function closeRoom(){activeId=null;lastMessageId=null;$('chatRoom').classList.add('hidden');$('chatWelcome').classList.remove('hidden');document.querySelector('.chat-shell').classList.remove('room-open');renderConversations()}
function bind(){ $('logout').onclick=async()=>{await fetch('/api/logout',{method:'POST'});location='/'};$('newChatBtn').onclick=showModal;$('startChatBtn').onclick=showModal;$('closeChatModal').onclick=closeModal;$('userSearch').oninput=renderUsers;$('chatSearch').oninput=renderConversations;$('messageForm').onsubmit=sendMessage;$('groupPanel').onsubmit=createGroup;$('mobileBack').onclick=closeRoom;$('messageInput').addEventListener('keydown',e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();sendMessage(e)}});document.querySelectorAll('.chat-tab').forEach(tab=>tab.onclick=()=>{document.querySelectorAll('.chat-tab').forEach(x=>x.classList.remove('active'));tab.classList.add('active');const group=tab.dataset.tab==='group';$('directPanel').classList.toggle('hidden',group);$('groupPanel').classList.toggle('hidden',!group)});pollTimer=setInterval(async()=>{if(document.hidden)return;try{await loadConversations();if(activeId)await loadMessages(false)}catch{}},2500)}
(async()=>{const me=await api('/api/me');myId=me.user?.id||me.user?._id?.toString()||'';await init()})().catch(()=>location='/login.html');
